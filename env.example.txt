# ═══════════════════════════════════════════════════════════
#  miniCRM — переменные окружения
#  Скопируйте файл: cp .env.example .env
#  НЕ коммитьте .env в Git!
# ═══════════════════════════════════════════════════════════

# ── Сервер ───────────────────────────────────────────────────
NODE_ENV=development          # development | production
PORT=3000

# ── PostgreSQL ───────────────────────────────────────────────
DB_HOST=localhost             # В Docker: postgres  |  Render: внешний host
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_strong_password_here
DB_NAME=minicrm

# ── Redis ─────────────────────────────────────────────────────
REDIS_HOST=127.0.0.1         # В Docker: redis  |  Upstash: hostname
REDIS_PORT=6379
REDIS_PASSWORD=               # Оставьте пустым если нет пароля

# ── JWT ───────────────────────────────────────────────────────
JWT_SECRET=replace_me_with_64_random_chars
JWT_EXPIRES_IN=7d

# ── Email (опционально) ───────────────────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@gmail.com
SMTP_PASS=your_app_password

# ── CORS (через запятую для продакшена) ───────────────────────
CORS_ORIGINS=http://localhost:3000,http://localhost:5501
